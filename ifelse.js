const prompt = require('prompt-sync')();
// const name=prompt("Enter the name:");
// console.log(`hey there is a ${name}`);

const number =prompt("Enter the number:");
if(number>0){
     console.log("The number is valid");
}
else{
     console.log("The number is naot valid")
}